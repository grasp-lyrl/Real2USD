"""R2S3D v2 core library (ROS-free).

Asset-centric metric-semantic mapping: SequenceSource data adapters, frame
conventions, object tracks, Sim(3) registration, multi-view refinement, and a
public-benchmark evaluation harness. ROS2 nodes import this package; it never
imports ROS.

See docs/REWORK_PLAN.md and docs/PHASE_SPECS.md at the repo root.
"""

__version__ = "0.1.0"
